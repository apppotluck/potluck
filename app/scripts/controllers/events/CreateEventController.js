define(['app'], function(app)
{
    app.controller('CreateEventController',
        [
            '$scope',
            '$location',
            function($scope,API,$location) {
            	
            }
        ]
    )
});       
